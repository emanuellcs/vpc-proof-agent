// Package netutil provides reusable low-level networking utilities for
// the vpc-proof agent.
//
// It includes default-route and gateway detection, DNS resolution
// helpers, outbound HTTPS connectivity checks, and HTTP request helpers
// shared by the probe package and external echo-service verification.
package netutil
